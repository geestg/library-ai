- [ ] Add router vision: include `routes_vision.router` into `backend/app/main.py`.
- [ ] Restart backend and verify endpoints:
  - [ ] GET `/upload-image` returns not-404
  - [ ] POST `/api/vision/chat` returns not-404
- [ ] Verify frontend multimodal flow with image attachment; ensure chat shows vision response.

